import AboutPage from '@/components/AboutPage'; // Import the component containing your about content
import React from 'react';

// This is the page component for the /about route
export default function AboutRoutePage() {
  // Render the existing AboutPage component here
  return <AboutPage />;
}
